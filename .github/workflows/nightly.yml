name: "🌙 Nightly"

on:
  schedule:
    - cron: "0 4 * * *"
  workflow_dispatch:

jobs:
  test-race:
    name: "🧪 Race Tests"
    runs-on: ubuntu-latest
    steps:
      - name: "📥 Checkout"
        uses: actions/checkout@v4
      - name: "🧰 Setup mise"
        uses: jdx/mise-action@v2
      - name: "🧪 task test:race"
        run: task test:race
