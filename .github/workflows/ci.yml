name: "🤖 CI"

on:
  push:
    branches: ["main"]
  pull_request:
  workflow_dispatch:

jobs:
  lint:
    name: "🧹 Lint"
    runs-on: ubuntu-latest
    steps:
      - name: "📥 Checkout"
        uses: actions/checkout@v4
      - name: "🧰 Setup mise"
        uses: jdx/mise-action@v2
      - name: "🧹 task lint"
        run: task lint

  test:
    name: "🧪 Test"
    runs-on: ubuntu-latest
    steps:
      - name: "📥 Checkout"
        uses: actions/checkout@v4
      - name: "🧰 Setup mise"
        uses: jdx/mise-action@v2
      - name: "🧪 task test"
        run: task test

  build:
    name: "🏗️ Build"
    runs-on: ubuntu-latest
    steps:
      - name: "📥 Checkout"
        uses: actions/checkout@v4
      - name: "🧰 Setup mise"
        uses: jdx/mise-action@v2
      - name: "🏗️ task build"
        run: task build

  examples:
    name: "📦 Examples"
    runs-on: ubuntu-latest
    steps:
      - name: "📥 Checkout"
        uses: actions/checkout@v4
      - name: "🧰 Setup mise"
        uses: jdx/mise-action@v2
      - name: "📦 task examples"
        run: task examples
